{% extends "base.html.twig" %}

{% block class %}dashboard{% endblock %}

{% block body %}
{# Navigation #}
    <section class="mb-5">
        <div class="container">
        <h2 class="mb-5">Administration</h2>
            <ul class="nav nav-tabs">
                <li class="nav-item">
                    <a class="nav-link {% if crud_article is defined %}active{% endif %}" href="{{ path('app_article_index') }}">Articles</a>
                </li>
                <li class="nav-item dropdown">
                    <a class="dropdown-toggle nav-dropdown nav-link {% if crud_teammates is defined %}active{% endif %}" data-bs-toggle="dropdown" aria-expanded="false">
                        Equipes
                    </a>
                    <ul class="dropdown-menu">
                        <li><a class="dropdown-item" href="{{ path('app_teammate_index', {'category': 'periscolaire'}) }}">Accueil périscolaire</a></li>
                        <li><a class="dropdown-item" href="{{ path('app_teammate_index', {'category': 'loisirs'}) }}">Accueil de loisirs</a></li>
                        <li><a class="dropdown-item" href="{{ path('app_teammate_index', {'category': 'association'}) }}">Association</a></li>
                    </ul>
                </li>
                {# <li class="nav-item">
                    <a class="nav-link disabled text-secondary {% if users %}active{% endif %}" href="#">Coin enfants</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link disabled text-secondary {% if users %}active{% endif %}" href="#">Coin parents</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link disabled text-secondary {% if users %}active{% endif %}" href="#">Infos pratiques</a>
                </li>
                {# <li class="nav-item">
                    <a class="nav-link" href="page/compte">Mon compte</a>
                </li> #}
                {# <li class="nav-item">
                    <a class="nav-link {% if users %}active{% endif %}" href="page/utilisateurs">Utilisateurs du site</a>
                </li> #}
            </ul>
        </div>
    </section>
{# contenu #}
    <section class="list">
        <div class="container">
            {% block content %}{% endblock %}
        </div>
    </section>

{% endblock %}