{% extends 'admin/dashboard.html.twig' %}

{% block title %}Administration{% endblock %}

{% block breadcrumb %}
    <li class="breadcrumb-item"><a href="{{ path('app_admin_periscolaire') }}" class="text-dark">Accueil périscolaire</a></li>
{% endblock %}

{% block content %}
    <h3>L'accueil périscolaire</h3>
    <div class="row">
        <div class="col-md-3 mb-3">
            <a href="{{ path('app_information_edit', {'label': 'app_page_periscolaire_description'}) }}">
                <div class="card bg-4 border-0 p-3 zoom-on-hover">
                    <div class="card-body text-center">
                        Description
                    </div>
                </div>
            </a>
        </div>
        <div class="col-md-3 mb-3">
            <a href="{{ path('app_teammate_index', {'category': 'periscolaire'}) }}">
                <div class="card bg-1 border-0 p-3 zoom-on-hover">
                    <div class="card-body text-center">
                        Equipe périscolaire
                    </div>
                </div>
            </a>
        </div>
        <div class="col-md-3 mb-3">
            <a href="{{ path('app_information_edit', {'label': 'app_page_periscolaire_time'}) }}">
                <div class="card bg-2 border-0 p-3 zoom-on-hover">
                    <div class="card-body text-center">
                        Horaires
                    </div>
                </div>
            </a>
        </div>
        {# <div class="col-md-3 mb-3">
            <a href="#">
                <div class="card bg-3 border-0 p-3 zoom-on-hover">
                    <div class="card-body text-center">
                        Tarifs
                    </div>
                </div>
            </a>
        </div> #}
    </div>
{% endblock %}