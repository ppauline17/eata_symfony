<form method="post" action="{{ path('app_teammate_delete', {'id': teammate.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ teammate.id) }}">
    <button class="btn">Delete</button>
</form>
