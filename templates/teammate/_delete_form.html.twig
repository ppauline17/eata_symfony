<form method="post" action="{{ path('app_teammate_delete', {'id': teammate.id}) }}" onsubmit="return confirm('Etes vous certain(e) de vouloir supprimer cette personne ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ teammate.id) }}">
    <button class="btn" title="Supprimer">{{ include('icons/delete.html.twig') }}</button>
</form>
