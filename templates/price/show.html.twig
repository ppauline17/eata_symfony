{% extends 'base.html.twig' %}

{% block title %}Price{% endblock %}

{% block body %}
    <h1>Price</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ price.id }}</td>
            </tr>
            <tr>
                <th>FamilyQuotient</th>
                <td>{{ price.familyQuotient }}</td>
            </tr>
            <tr>
                <th>HalfDay</th>
                <td>{{ price.halfDay }}</td>
            </tr>
            <tr>
                <th>FullDay</th>
                <td>{{ price.fullDay }}</td>
            </tr>
            <tr>
                <th>Morning</th>
                <td>{{ price.morning }}</td>
            </tr>
            <tr>
                <th>EveningFirstHour</th>
                <td>{{ price.eveningFirstHour }}</td>
            </tr>
            <tr>
                <th>EveningHalfHour</th>
                <td>{{ price.eveningHalfHour }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_price_index') }}">back to list</a>

    <a href="{{ path('app_price_edit', {'id': price.id}) }}">edit</a>

    {{ include('price/_delete_form.html.twig') }}
{% endblock %}
